import java.util.*;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonParser;

/**
 * Problem-specific driver for Longest Substring Without Repeating Characters problem.
 */
class LongestSubstringDriver extends ProblemDriverBase {
    @Override
    public List<TestCase> parseTestCases(JsonArray input, JsonArray expectedOutput) {
        List<TestCase> testCases = new ArrayList<>();
        
        for (int i = 0; i < input.size(); i++) {
            String s = input.get(i).getAsString();
            int expected = expectedOutput.get(i).getAsInt();
            
            testCases.add(new TestCase(s, expected));
        }
        
        return testCases;
    }

    @Override
    public Object executeTestCase(TestCase testCase, int index) throws Exception {
        String s = (String) testCase.getInput();
        return Solution.lengthOfLongestSubstring(s);
    }

    @Override
    public boolean compareResults(Object actual, Object expected) {
        return Objects.equals(actual, expected);
    }
}

class Program {
    public static void main(String[] args) {
        // Create and run the driver
        BaseDriver driver = new BaseDriver(new LongestSubstringDriver());
        driver.run(args);
    }
}
